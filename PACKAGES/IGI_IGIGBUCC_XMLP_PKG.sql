--------------------------------------------------------
--  DDL for Package IGI_IGIGBUCC_XMLP_PKG
--------------------------------------------------------

  CREATE OR REPLACE EDITIONABLE PACKAGE "APPS"."IGI_IGIGBUCC_XMLP_PKG" AUTHID CURRENT_USER AS
/* $Header: IGIGBUCCS.pls 120.0.12010000.1 2008/07/29 08:58:11 appldev ship $ */
  P_DEBUG_SWITCH VARCHAR2(1);

  P_RUN_AOL VARCHAR2(1);

  P_MIN_FLEX VARCHAR2(1000);

  P_MAX_FLEX VARCHAR2(1000);

  P_SET_OF_BOOKS_ID NUMBER;

  P_CONC_REQUEST_ID NUMBER;

  P_COA_ID NUMBER;

  CP_SELECT_ALL VARCHAR2(4000) := '(CC.SEGMENT1 || ''-'' || SEGMENT2 || ''-'' || SEGMENT3
  || ''-'' || SEGMENT4 || ''-'' || SEGMENT5 || ''-'' || SEGMENT6 || ''-'' || SEGMENT7
  || ''-'' || SEGMENT8 || ''-'' || SEGMENT9 || ''-'' || SEGMENT10 || ''-'' || SEGMENT11
  || ''-'' || SEGMENT12 || ''-'' || SEGMENT13 || ''-'' || SEGMENT14 || ''-'' || SEGMENT15
  || ''-'' || SEGMENT16 || ''-'' || SEGMENT17 || ''-'' || SEGMENT18 || ''-'' || SEGMENT19
  || ''-'' || SEGMENT20 || ''-'' || SEGMENT21 || ''-'' || SEGMENT22 || ''-'' || SEGMENT23
  || ''-'' || SEGMENT24 || ''-'' || SEGMENT25 || ''-'' || SEGMENT26 || ''-'' || SEGMENT27
  || ''-'' || SEGMENT28 || ''-'' || SEGMENT29 || ''-'' || SEGMENT30)';

  CP_WHERE_FLEX VARCHAR2(5000) := 'CC.SEGMENT3 BETWEEN ''5000'' AND ''5900''';

  CP_NO_DATA_FOUND VARCHAR2(1);

  FUNCTION BEFOREREPORT RETURN BOOLEAN;

  FUNCTION AFTERREPORT RETURN BOOLEAN;

  FUNCTION CF_UPDATEFORMULA(CCID IN NUMBER) RETURN NUMBER;

  FUNCTION CF_NO_DATA_FOUNDFORMULA(NO_DATA IN VARCHAR2) RETURN NUMBER;

  FUNCTION CP_SELECT_ALL_P RETURN VARCHAR2;

  FUNCTION CP_WHERE_FLEX_P RETURN VARCHAR2;

  FUNCTION CP_NO_DATA_FOUND_P RETURN VARCHAR2;

END IGI_IGIGBUCC_XMLP_PKG;

/
