--------------------------------------------------------
--  DDL for Package BOM_CSTRELCR_XMLP_PKG
--------------------------------------------------------

  CREATE OR REPLACE EDITIONABLE PACKAGE "APPS"."BOM_CSTRELCR_XMLP_PKG" AUTHID CURRENT_USER AS
/* $Header: CSTRELCRS.pls 120.0 2007/12/24 09:58:43 dwkrishn noship $ */
  REPORT_SORT_OPT NUMBER;

  COST_TYPE_ID NUMBER;

  ORG_ID NUMBER;

  P_CONC_REQUEST_ID NUMBER := 0;

  P_FLEXDATA_ITEM VARCHAR2(850);

  P_FLEXDATA_CAT VARCHAR2(850);

  P_STRUCT_NUM NUMBER;
  pid_flex_num NUMBER;

  P_WHERE_ITEM VARCHAR2(850) := '1 = 1';

  P_FROM_ITEM VARCHAR2(80);

  P_TO_ITEM VARCHAR2(80);

  P_WHERE_CAT VARCHAR2(850) := '1 = 1';

  P_FROM_CAT VARCHAR2(80);

  P_TO_CAT VARCHAR2(80);

  P_CATEGORY_SET NUMBER;

  ZERO_COST_ONLY NUMBER;

  WHERE_ZERO VARCHAR2(40) := '1 = 1';

  P_VIEW_COST NUMBER;

  P_TRACE_FLAG NUMBER;

  P_EXCHANGE_RATE NUMBER := 1;

  P_CURRENCY_CODE VARCHAR2(15);

  P_EXCHANGE_RATE_CHAR VARCHAR2(38);

  FUNCTION BEFOREREPORT RETURN BOOLEAN;

  FUNCTION AFTERREPORT RETURN BOOLEAN;

  FUNCTION DISP_CURRENCYFORMULA(CURR_CODE_SAVED IN VARCHAR2) RETURN VARCHAR2;

  FUNCTION ORDER_FUNC(C_FLEXPAD_ITEM IN VARCHAR2
                     ,C_FLEXPAD_CAT IN VARCHAR2) RETURN CHARACTER;

  FUNCTION C_FLEXPAD_ITEMFORMULA(C_FLEXFIELD_ITEM IN VARCHAR2
                                ,C_FLEXPAD_ITEM IN VARCHAR2) RETURN VARCHAR2;

  FUNCTION C_FLEXPAD_CATFORMULA(C_FLEXFIELD_CAT IN VARCHAR2
                               ,C_FLEXPAD_CAT IN VARCHAR2) RETURN VARCHAR2;

  FUNCTION AFTERPFORM RETURN BOOLEAN;

END BOM_CSTRELCR_XMLP_PKG;


/
