--------------------------------------------------------
--  DDL for Package Body QA_QLTCHWUR_XMLP_PKG
--------------------------------------------------------

  CREATE OR REPLACE EDITIONABLE PACKAGE BODY "APPS"."QA_QLTCHWUR_XMLP_PKG" AS
/* $Header: QLTCHWURB.pls 120.0 2007/12/24 10:33:42 krreddy noship $ */
  FUNCTION AFTERPFORM RETURN BOOLEAN IS
  BEGIN
    IF (P_CHARACTERISTIC IS NOT NULL) THEN
      SELECT
        NAME
      INTO P_CHARACTERISTIC
      FROM
        QA_CHARS
      WHERE CHAR_ID = P_CHARACTERISTIC;
      P_CHARACTERISTIC_LIMITER := 'and qc.name = ''' || P_CHARACTERISTIC || '''';
    END IF;
    IF (P_ORGANIZATION_ID IS NOT NULL) THEN
      P_ORG_DELIMITER := 'and qpcv.organization_id = ' || TO_CHAR(P_ORGANIZATION_ID);
    END IF;
    IF (P_CHAR_ENABLED IS NOT NULL) THEN
      P_CHAR_ENABLED_DELIMITER := 'and ml.lookup_code = ''' || P_CHAR_ENABLED || '''';
      SELECT
        MEANING
      INTO P_CHAR_ENABLED_MEANING
      FROM
        MFG_LOOKUPS
      WHERE LOOKUP_TYPE = 'SYS_YES_NO'
        AND LOOKUP_CODE = P_CHAR_ENABLED;
    END IF;
    IF (P_PLAN_CHAR_ENABLED IS NOT NULL) THEN
      P_PLAN_ENABLED_DELIMITER := 'and qpcv.enabled_flag = ''' || P_PLAN_CHAR_ENABLED || '''';
      SELECT
        MEANING
      INTO P_PLAN_ENABLED_MEANING
      FROM
        MFG_LOOKUPS
      WHERE LOOKUP_TYPE = 'SYS_YES_NO'
        AND LOOKUP_CODE = P_PLAN_CHAR_ENABLED;
    END IF;
    LP_CHARACTERISTIC:=P_CHARACTERISTIC;
    LP_CHAR_ENABLED_MEANING:=P_CHAR_ENABLED_MEANING;
    LP_PLAN_ENABLED_MEANING:=P_PLAN_ENABLED_MEANING;
    RETURN (TRUE);
  END AFTERPFORM;

  FUNCTION C_DEF_VALUE_NUMBERFORMULA(DATATYPE1 IN NUMBER
                                    ,DEFAULT_VALUE1 IN VARCHAR2) RETURN NUMBER IS
  BEGIN
    IF (DATATYPE1 = 2) THEN
      RETURN (TO_NUMBER(DEFAULT_VALUE1));
    ELSE
      RETURN (2);
    END IF;
    RETURN NULL;
  END C_DEF_VALUE_NUMBERFORMULA;

  FUNCTION BEFOREREPORT RETURN BOOLEAN IS
  BEGIN
    P_CONC_REQUEST_ID := FND_GLOBAL.CONC_REQUEST_ID;
    /*SRW.USER_EXIT('FND SRWINIT')*/NULL;
    RETURN (TRUE);
  END BEFOREREPORT;

  FUNCTION AFTERREPORT RETURN BOOLEAN IS
  BEGIN
    /*SRW.USER_EXIT('FND SRWEXIT')*/NULL;
    RETURN (TRUE);
  END AFTERREPORT;

END QA_QLTCHWUR_XMLP_PKG;


/
