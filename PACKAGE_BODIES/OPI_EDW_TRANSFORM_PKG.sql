--------------------------------------------------------
--  DDL for Package Body OPI_EDW_TRANSFORM_PKG
--------------------------------------------------------

  CREATE OR REPLACE EDITIONABLE PACKAGE BODY "APPS"."OPI_EDW_TRANSFORM_PKG" AS
	/*$Header: OPIMDECB.pls 115.6 2002/04/29 15:24:09 pkm ship     $ */
Function OPI_REV_PROD_DECODE(ITEM_FK_KEY  NUMBER,
                             LINE_CONTEXT VARCHAR,
                             PARENT_ITEM_FK_KEY NUMBER,
                             DATA_VALUE NUMBER) RETURN NUMBER IS
BEGIN
   IF LINE_CONTEXT = 'ORDER ENTRY' AND ITEM_FK_KEY <> PARENT_ITEM_FK_KEY  THEN
      RETURN 0;
   ELSE
      RETURN NVL(DATA_VALUE,0);
   END IF;
EXCEPTION WHEN OTHERS THEN
  RETURN 0;
End OPI_REV_PROD_DECODE;
Function OPI_IPS_BEG_BAL_DECODE(PERIOD_FLAG  NUMBER,
                            DATA_VALUE NUMBER) RETURN NUMBER IS
BEGIN
  IF PERIOD_FLAG = 0 THEN
    RETURN DATA_VALUE;
  ELSE
    RETURN 0;
  END IF;
EXCEPTION WHEN OTHERS THEN
  RETURN 0;
END OPI_IPS_BEG_BAL_DECODE;
Function OPI_IPS_END_BAL_DECODE(PERIOD_FLAG  NUMBER,
                            DATA_VALUE NUMBER) RETURN NUMBER IS
BEGIN
  IF PERIOD_FLAG = 1 THEN
    RETURN DATA_VALUE;
  ELSE
    RETURN 0;
  END IF;
EXCEPTION WHEN OTHERS THEN
  RETURN 0;
END OPI_IPS_END_BAL_DECODE;

END OPI_EDW_TRANSFORM_PKG;

/
