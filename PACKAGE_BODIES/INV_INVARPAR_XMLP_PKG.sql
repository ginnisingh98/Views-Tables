--------------------------------------------------------
--  DDL for Package Body INV_INVARPAR_XMLP_PKG
--------------------------------------------------------

  CREATE OR REPLACE EDITIONABLE PACKAGE BODY "APPS"."INV_INVARPAR_XMLP_PKG" AS
/* $Header: INVARPARB.pls 120.3 2008/01/08 06:21:53 dwkrishn noship $ */
  FUNCTION BEFOREREPORT RETURN BOOLEAN IS
  BEGIN
    BEGIN
      P_CONC_REQUEST_ID := FND_GLOBAL.CONC_REQUEST_ID;
      /*SRW.USER_EXIT('FND SRWINIT')*/NULL;
    EXCEPTION
      WHEN /*SRW.USER_EXIT_FAILURE*/OTHERS THEN
        /*SRW.MESSAGE(1
                   ,'Failed in before report trigger:SRWINIT')*/NULL;
        RAISE;
    END;
    DECLARE
      P_ORG_ID_CHAR VARCHAR2(100) := TO_CHAR(P_ORG_ID);
    BEGIN
      FND_PROFILE.PUT('MFG_ORGANIZATION_ID'
                     ,P_ORG_ID_CHAR);
      /*SRW.USER_EXIT('FND PUTPROFILE NAME="' || 'MFG_ORGANIZATION_ID' || '" FIELD="' || P_ORG_ID_CHAR || '"')*/NULL;
    EXCEPTION
      WHEN /*SRW.USER_EXIT_FAILURE*/OTHERS THEN
        /*SRW.MESSAGE(020
                   ,'Failed in before report trigger, setting org profile ')*/NULL;
        RAISE;
    END;
    BEGIN
      SELECT
        MEANING
      INTO P_REJ_OPT_TITLE
      FROM
        MFG_LOOKUPS
      WHERE LOOKUP_TYPE = 'SYS_YES_NO'
        AND LOOKUP_CODE = NVL(P_INCLUDE_REJ_ITEMS
         ,1);
    EXCEPTION
      WHEN OTHERS THEN
        NULL;
    END;
    IF NVL(P_INCLUDE_REJ_ITEMS
       ,1) = 1 THEN
      P_INCLUDE_REJ_ITEMS_OPT := ' ';
    ELSE
      P_INCLUDE_REJ_ITEMS_OPT := 'and nvl(mpa.approval_status,1) <> 2 ';
    END IF;
    BEGIN
      NULL;
    EXCEPTION
      WHEN /*SRW.USER_EXIT_FAILURE*/OTHERS THEN
        /*SRW.MESSAGE(1
                   ,'Failed in before report trigger:MSTK')*/NULL;
        RAISE;
    END;
    BEGIN
      NULL;
    EXCEPTION
      WHEN /*SRW.USER_EXIT_FAILURE*/OTHERS THEN
        /*SRW.MESSAGE(1
                   ,'Failed in before report trigger:MSTK:ORDERBY')*/NULL;
        RAISE;
    END;
    BEGIN
      NULL;
    EXCEPTION
      WHEN /*SRW.USER_EXIT_FAILURE*/OTHERS THEN
        /*SRW.MESSAGE(1
                   ,'Failed in before report trigger:MTLL')*/NULL;
        RAISE;
    END;
    DECLARE
      M_RETURN_STATUS VARCHAR2(1);
      M_MSG_COUNT NUMBER;
      M_MSG_DATA VARCHAR2(2000);
    BEGIN
      IF (WMS_INSTALL.CHECK_INSTALL(X_RETURN_STATUS => M_RETURN_STATUS
                               ,X_MSG_COUNT => M_MSG_COUNT
                               ,X_MSG_DATA => M_MSG_DATA
                               ,P_ORGANIZATION_ID => P_ORG_ID)) THEN
        P_WMS_INSTALLED := 'TRUE';
      END IF;
    END;
    RETURN (TRUE);
  END BEFOREREPORT;

  FUNCTION AFTERREPORT RETURN BOOLEAN IS
  BEGIN
    BEGIN
      /*SRW.USER_EXIT('FND SRWEXIT')*/NULL;
    EXCEPTION
      WHEN /*SRW.USER_EXIT_FAILURE*/OTHERS THEN
        /*SRW.MESSAGE(1
                   ,'SRWEXIT failed')*/NULL;
    END;
    RETURN (TRUE);
  END AFTERREPORT;

  FUNCTION C_PHYS_INV_NAMEFORMULA RETURN VARCHAR2 IS
  BEGIN
    DECLARE
      NAME VARCHAR2(40);
    BEGIN
      SELECT
        PHYSICAL_INVENTORY_NAME
      INTO NAME
      FROM
        MTL_PHYSICAL_INVENTORIES
      WHERE ORGANIZATION_ID = P_ORG_ID
        AND PHYSICAL_INVENTORY_ID = P_PHYS_INV_ID;
      RETURN (NAME);
    EXCEPTION
      WHEN NO_DATA_FOUND THEN
        RETURN (NULL);
    END;
    RETURN NULL;
  END C_PHYS_INV_NAMEFORMULA;

  FUNCTION C_CURRENCY_CODEFORMULA(R_CURRENCY_CODE IN VARCHAR2) RETURN VARCHAR2 IS
  BEGIN
    RETURN ('(' || R_CURRENCY_CODE || ')');
  END C_CURRENCY_CODEFORMULA;

  FUNCTION C_SORTFORMULA RETURN VARCHAR2 IS
  BEGIN
    IF P_SORT_ID = 1 THEN
      RETURN ('ASC');
    ELSE
      RETURN ('DESC');
    END IF;
    RETURN NULL;
  END C_SORTFORMULA;

  FUNCTION CF_COST_GROUPFORMULA(COST_GROUP_ID IN NUMBER) RETURN CHAR IS
    M_COST_GROUP VARCHAR2(10) := NULL;
  BEGIN
    IF ((P_WMS_INSTALLED = 'TRUE') AND (COST_GROUP_ID IS NOT NULL)) THEN
      BEGIN
        SELECT
          COST_GROUP
        INTO M_COST_GROUP
        FROM
          CST_COST_GROUPS
        WHERE COST_GROUP_ID = COST_GROUP_ID;
      EXCEPTION
        WHEN NO_DATA_FOUND THEN
          M_COST_GROUP := 'ERROR';
      END;
    END IF;
    RETURN (M_COST_GROUP);
  END CF_COST_GROUPFORMULA;

  FUNCTION CF_OUTERMOST_LPNFORMULA(OUTERMOST_LPN_ID IN NUMBER) RETURN CHAR IS
    M_OUTERMOST_LPN VARCHAR2(30) := NULL;
  BEGIN
    IF ((P_WMS_INSTALLED = 'TRUE') AND (OUTERMOST_LPN_ID IS NOT NULL)) THEN
      BEGIN
        SELECT
          LICENSE_PLATE_NUMBER
        INTO M_OUTERMOST_LPN
        FROM
          WMS_LICENSE_PLATE_NUMBERS
        WHERE LPN_ID = OUTERMOST_LPN_ID;
      EXCEPTION
        WHEN NO_DATA_FOUND THEN
          M_OUTERMOST_LPN := 'ERROR';
      END;
    END IF;
    RETURN (M_OUTERMOST_LPN);
  END CF_OUTERMOST_LPNFORMULA;

  FUNCTION CF_PARENT_LPNFORMULA(PARENT_LPN_ID IN NUMBER) RETURN CHAR IS
    M_PARENT_LPN VARCHAR2(30) := NULL;
  BEGIN
    IF ((P_WMS_INSTALLED = 'TRUE') AND (PARENT_LPN_ID IS NOT NULL)) THEN
      BEGIN
        SELECT
          LICENSE_PLATE_NUMBER
        INTO M_PARENT_LPN
        FROM
          WMS_LICENSE_PLATE_NUMBERS
        WHERE LPN_ID = PARENT_LPN_ID;
      EXCEPTION
        WHEN NO_DATA_FOUND THEN
          M_PARENT_LPN := 'ERROR';
      END;
    END IF;
    RETURN (M_PARENT_LPN);
  END CF_PARENT_LPNFORMULA;

  FUNCTION CF_APPROVER_FORMULA(EMP_ID IN NUMBER) RETURN CHAR IS
    V_EMP_NAME VARCHAR2(240) := NULL;
  BEGIN
    IF (EMP_ID IS NOT NULL) THEN
      BEGIN
        SELECT
          FULL_NAME
        INTO V_EMP_NAME
        FROM
          MTL_EMPLOYEES_VIEW
        WHERE ORGANIZATION_ID = P_ORG_ID
          AND EMPLOYEE_ID = EMP_ID;
      EXCEPTION
        WHEN NO_DATA_FOUND THEN
          V_EMP_NAME := 'ERROR';
      END;
    END IF;
    RETURN (V_EMP_NAME);
  END CF_APPROVER_FORMULA;

  FUNCTION CF_APPROVERFORMULA(EMP_ID IN NUMBER) RETURN CHAR IS
    V_EMP_NAME VARCHAR2(240) := NULL;
  BEGIN
    IF (EMP_ID IS NOT NULL) THEN
      BEGIN
        SELECT
          FULL_NAME
        INTO V_EMP_NAME
        FROM
          MTL_EMPLOYEES_VIEW
        WHERE ORGANIZATION_ID = P_ORG_ID
          AND EMPLOYEE_ID = EMP_ID;
      EXCEPTION
        WHEN NO_DATA_FOUND THEN
          V_EMP_NAME := 'ERROR';
      END;
    END IF;
    RETURN (V_EMP_NAME);
  END CF_APPROVERFORMULA;

END INV_INVARPAR_XMLP_PKG;


/
